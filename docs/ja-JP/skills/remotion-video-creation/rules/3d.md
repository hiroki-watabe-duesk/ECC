---
name: 3d
description: Three.js と React Three Fiber を使用した Remotion の 3D コンテンツ。
metadata:
  tags: 3d, three, threejs
---

# Remotion で Three.js と React Three Fiber を使用する

React Three Fiber および Three.js のベストプラクティスに従ってください。
以下に示す Remotion 固有のルールのみを追加で遵守する必要があります。

## 前提条件

まず、`@remotion/three` パッケージをインストールする必要があります。
インストールされていない場合は、以下のコマンドを使用してください。

```bash
npx remotion add @remotion/three # npm を使用するプロジェクトの場合
bunx remotion add @remotion/three # bun を使用するプロジェクトの場合
yarn remotion add @remotion/three # yarn を使用するプロジェクトの場合
pnpm exec remotion add @remotion/three # pnpm を使用するプロジェクトの場合
```

## ThreeCanvas の使用

3D コンテンツは必ず `<ThreeCanvas>` でラップし、適切なライティングを含める必要があります。
`<ThreeCanvas>` には `width` と `height` プロップが必須です。

```tsx
import { ThreeCanvas } from "@remotion/three";
import { useVideoConfig } from "remotion";

const { width, height } = useVideoConfig();

<ThreeCanvas width={width} height={height}>
  <ambientLight intensity={0.4} />
  <directionalLight position={[5, 5, 5]} intensity={0.8} />
  <mesh>
    <sphereGeometry args={[1, 32, 32]} />
    <meshStandardMaterial color="red" />
  </mesh>
</ThreeCanvas>
```

## `useCurrentFrame()` で駆動しないアニメーションの禁止

シェーダーやモデルなどは、自律的にアニメーションさせてはなりません。
`useCurrentFrame()` で駆動するアニメーション以外は一切使用できません。
それ以外のアニメーションはレンダリング時にちらつきを引き起こします。

`@react-three/fiber` の `useFrame()` の使用は禁止されています。

## `useCurrentFrame()` を使ったアニメーション

アニメーションの実装には `useCurrentFrame()` を使用してください。

```tsx
const frame = useCurrentFrame();
const rotationY = frame * 0.02;

<mesh rotation={[0, rotationY, 0]}>
  <boxGeometry args={[2, 2, 2]} />
  <meshStandardMaterial color="#4a9eff" />
</mesh>
```

## `<ThreeCanvas>` 内での `<Sequence>` の使用

`<ThreeCanvas>` 内に配置する `<Sequence>` の `layout` プロップは必ず `none` に設定してください。

```tsx
import { Sequence } from "remotion";
import { ThreeCanvas } from "@remotion/three";

const { width, height } = useVideoConfig();

<ThreeCanvas width={width} height={height}>
  <Sequence layout="none">
    <mesh>
      <boxGeometry args={[2, 2, 2]} />
      <meshStandardMaterial color="#4a9eff" />
    </mesh>
  </Sequence>
</ThreeCanvas>
```
